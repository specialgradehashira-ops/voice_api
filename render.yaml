services:
  - type: web
    name: voice-api
    env: python
    plan: free
    buildCommand: |
      apt-get update && \
      apt-get install -y ffmpeg espeak-ng && \
      pip install -r requirements.txt
    startCommand: python app.py
